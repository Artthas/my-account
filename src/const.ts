export enum AppRoute {
  Root = '/',
  SignIn = '/sign-in',
  SignUp = '/sign-up',
}

export enum AuthorizationStatus {
  Auth = 'AUTH',
  NoAuth = 'NO_AUTH',
  Unknown = 'UNKNOWN',
}

export enum APIRoute {
  Contacts = '/contacts',
  SignIn = '/signin',
  SignUp = '/users',
}
